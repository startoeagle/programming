#include "lu.h"

matrix * matrix_init( int nr_row, int nr_col){
  matrix * res = malloc(sizeof(int)*nr_row*nr_col);
}



matrix * lu(matrix * m){
  // perfoms an LU decomposition and returns the L and U

  // allocate for the l and u matrices
  matrix * res = malloc(2*sizeof(matrix));
}
